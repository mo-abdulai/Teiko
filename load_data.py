"""Placeholder entry point for loading the Teiko assessment dataset.

This script must remain at the repository root because the assessment expects
`python load_data.py` to work.
"""


def main() -> None:
    """Run the future SQLite database loading workflow."""
    # TODO: Initialize the SQLite database, create the schema, and load
    # cell-count.csv into normalized tables.
    print("Data loading placeholder: implementation is in progress.")


if __name__ == "__main__":
    main()
